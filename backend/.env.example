# backend/.env.example

# Required: Your Anthropic API key
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Override the Claude model (default: claude-opus-4-20250514)
# Available models:
#   claude-opus-4-20250514    - Most capable, best for nuanced conversations
#   claude-sonnet-4-20250514  - Balanced speed and capability
#   claude-3-5-sonnet-20241022 - Previous gen, still capable
#   claude-3-5-haiku-20241022  - Fast and lightweight
# CLAUDE_MODEL=claude-opus-4-20250514

# Optional: Override data paths
# DATABASE_PATH=./data/mentor.db
# CHROMA_PATH=./data/chroma

# Optional: Number of chunks to retrieve for context (default: 10)
# RETRIEVAL_TOP_K=10

# Optional: Chunking settings for ingestion
# CHUNK_TARGET_TOKENS=650
# CHUNK_MAX_TOKENS=800

# Optional: CORS allowed origins (comma-separated, default: http://localhost:5173)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
